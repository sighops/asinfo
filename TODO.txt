Don't maintain separate utilities for downloading and converting files.
Create one cli util that can be used for everything but move code into a shared lib that asinfo and the cli tool both use.
Allow people to use asinfo as just a fully featured cli tool and/or as a module in a larger app.

CLI needs to:

Be able to download and use it's own data
Give info on the age of the data used
Quickly load a cache of its most recent data for cli lookups
Keep only most recent copy of records for lookups

example commands:

asinfo lookup IP
asinfo name ASN
asinfo prefixes ASN
asinfo prefixesv6 ASN ???
asinfo download rib|names
asinfo update











DONE Support all methods for file downloading(ftp, http, https, etc.)
DONE Use pytricia